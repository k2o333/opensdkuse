{
  "name": "opencode-sdk-cli",
  "version": "0.1.0",
  "description": "OpenCode SDK CLI tool (MVP)",
  "type": "module",
  "scripts": {
    "dev": "node --loader ts-node/esm run.ts",
    "start": "node --loader ts-node/esm run.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --loader ts-node/esm --test test/*.test.ts"
  },
  "dependencies": {
    "@opencode-ai/sdk": "1.3.2"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "ts-node": "^10.0.0",
    "tslib": "^2.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.0.0"
  }
}
